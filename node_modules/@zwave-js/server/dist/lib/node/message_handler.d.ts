import { Driver } from "zwave-js";
import { Client, ClientsController } from "../server";
import { NodeCommand } from "./command";
import { IncomingMessageNode } from "./incoming_message";
import { NodeResultTypes } from "./outgoing_message";
export declare class NodeMessageHandler {
    handle(message: IncomingMessageNode, clientsController: ClientsController, driver: Driver, client: Client): Promise<NodeResultTypes[NodeCommand]>;
}
