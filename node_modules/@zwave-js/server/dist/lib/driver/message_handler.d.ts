import { Driver } from "zwave-js";
import { Client, ClientsController, Logger, ZwavejsServerRemoteController } from "../server";
import { DriverCommand } from "./command";
import { IncomingMessageDriver } from "./incoming_message";
import { DriverResultTypes } from "./outgoing_message";
export declare class DriverMessageHandler {
    static handle(message: IncomingMessageDriver, remoteController: ZwavejsServerRemoteController, clientsController: ClientsController, logger: Logger, driver: Driver, client: Client): Promise<DriverResultTypes[DriverCommand]>;
}
